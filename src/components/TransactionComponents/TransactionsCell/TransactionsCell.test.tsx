import {render, screen} from "@testing-library/react";
import "@testing-library/jest-dom";
import TransactionsCell from "./TransactionsCell"

it("Loads element without crashing", () => {
	// render(<TransactionsCell/>);
});
