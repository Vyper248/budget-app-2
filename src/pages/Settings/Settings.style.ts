import styled from "styled-components";

const StyledSettings = styled.div`
    & .settingsContainer {
        max-width: 350px;
        margin: auto;

        & > * {
            margin-bottom: 5px;
        }
    }
`;

export default StyledSettings
