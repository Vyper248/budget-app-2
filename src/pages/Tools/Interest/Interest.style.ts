import styled from "styled-components";

const StyledInterest = styled.div`
    & p {
        text-align: center;
        max-width: 1000px;
        margin: 20px auto;
    }
`;

export default StyledInterest
